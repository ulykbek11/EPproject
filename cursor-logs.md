75) Исправление ошибки 500/Non-2xx:
    - Переход на стабильную модель `gemini-1.5-flash` (вместо unstable `flash-latest`).
    - Ослабление строгой проверки JSON: убрано требование "ONLY return if counts match", которое могло вызывать отказ модели возвращать JSON при малейшем несовпадении.
    - Убрано поле `verification` из ожидаемого ответа JSON для упрощения парсинга.
    - Функция `analyze-gpa` переразвернута.
