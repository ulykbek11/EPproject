# EduPath

Проект на Vite + React (TypeScript). Для локального запуска:

- Установите зависимости: `npm install`
- Запустите dev-сервер: `npm run dev`

Файл `.env` не коммитится; используйте `.env.example` как шаблон.
